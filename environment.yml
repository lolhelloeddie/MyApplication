name: my-environment
channels:
  - defaults
dependencies:
  - python=3.8
  - numpy
  - pandas
  - scikit-learn
  - pip
  - pip:
      - matplotlib
      - seaborn
      - torch
      - transformers
      - sentence-transformers
