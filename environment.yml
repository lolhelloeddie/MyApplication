name: my-environment
channels:
  - defaults
dependencies:
  - python=3.8
  - numpy
  - pandas
  - scikit-learn
  - pip
  - pip:
      - pip install numpy pandas scikit-learn matplotlib seaborn # Replace with actual package name
      - pip install torch transformers sentence-transformers
